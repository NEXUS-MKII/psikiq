# PsikiQ home — sections for GHL

Original page, unmodified, cut at each section. Paste each file into its own
Custom Code element, top to bottom, in file order. `00-head.html` first (it
carries the stylesheet), `13-scripts.html` last.

`all-in-one.html` is the same content as a single drop.

Images point at the GitHub-hosted copies.
